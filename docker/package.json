{
    "name": "@regexp-library/docker",
    "version": "1.0.0",
    "private": true, 
    "workspaces": ["backend/*", "client/*", "docker/*"],
    "scripts": {
        "start": "docker compose -f docker-compose-dev.yml up -d --remove-orphans --build",
        "stop": "docker compose -f docker-compose-dev.yml down"
    }
}